name: Daily Commit

on:
  schedule:
    # Runs every day at 05:30 UTC (pick any fixed hour/minute you want)
    - cron: "30 5 * * *"
  workflow_dispatch:  # Allows manual trigger for testing

jobs:
  daily-commit-job:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Add commit by 21f1001577@ds.study.iitm.ac.in
        run: |
          echo "Automated commit on $(date -u)" >> daily-log.txt
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add daily-log.txt
          git commit -m "Daily automated commit [skip ci]" || echo "No changes to commit"
          git push
